plugins {
    id "com.android.application"
}

android {
    namespace "com.gitmartini.slither"
    compileSdk 35

    defaultConfig {
        applicationId "com.gitmartini.slither"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }

    applicationVariants.configureEach { variant ->
        outputs.configureEach {
            outputFileName = "slither-${variant.name}.apk"
        }
    }
}
